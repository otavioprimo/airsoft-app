export interface ThemeProps {
  theme: Theme;
}

interface Theme {
  primary: string;
  secondary: string;
  textPrimary: string;
  textSecondary: string;
  danger: string;
  success: string;
  warning: string;
  google: string;
  facebook: string;
}
